apiVersion: apps/v1
kind: Deployment
metadata:
  name:  "{{ .Release.Name }}"
  labels:
    {{- include "gss-single-template.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "gss-single-template.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "gss-single-template.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}        
      {{- end }}
      serviceAccountName: {{ .Values.github.knownHost | default "" | quote }}
      containers:
        - name: gss-single-template
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
